name: Agricy CI

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Verify repository
        run: |
          echo "Agricy CI started"
          ls -la
          echo "Success"